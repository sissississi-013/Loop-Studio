# Loop Studio

The user authorized building this open-source video editor through completion and updating https://github.com/sissississi-013/Loop-Studio. Work in this clean checkout. The historical CutLoop prototype lives at /Users/sissi/usagi and its frozen experiments must remain unchanged.

Read BUILD_STATUS.md before continuing. Finish concrete implementation gates with tests and real media/UI verification; do not claim creator validation, semantic understanding or aesthetic superiority from heuristics. The manual workflow must work without paid APIs. Natural-language and reference features must state their model requirements and fallbacks.

Push reviewed milestones to the authorized repository. Do not commit credentials, user uploads, private context, downloaded research media, or old experiment history. No public service deployment, paid subscriptions or messages to others are authorized. No subagent delegation is authorized.

The initial MVP gates are complete and the heartbeat `complete-loop-studio-mvp` is paused. Do not restart it without a new user request; report external blockers honestly. Avoid endless cosmetic iteration. Local task continuation requires the desktop execution environment to be available.

Use Python 3.12+ and FFmpeg/ffprobe. Prefer the standard library for the local core. Test changes with `python3 -m unittest discover -s tests -v`. Models are optional adapters; do not repeatedly retry the exhausted original OpenAI API. Modal compute, if needed: inspect live apps first, at most one task GPU, 30 minutes per job, two GPU hours before reassessment. Never stop unrelated apps.
